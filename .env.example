# Backend (a definir: Firebase / Supabase / SQLite)
# VITE_API_BASE=

# Proxy opcional para modelos 3D (contorna CORS) — herdado do padrão do livromagico
# VITE_MODEL_PROXY=/api/proxy-model

# Integração ZappFlow (leads/eventos) — a definir
# ZAPPFLOW_API_URL=
# ZAPPFLOW_API_KEY=
